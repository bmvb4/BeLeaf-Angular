import { ISearch } from "../Interface/isearch";

export class Search implements ISearch {
  forEach(arg0: (usr: any) => void) {
    throw new Error('Method not implemented.');
  }
  TagName: string;
  username: string;
  photo: any;
}
